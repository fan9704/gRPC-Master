syntax = "proto3"; // protocol buffer version
package api; // name space

// service 定義呼叫 API 的方法
service API{
  rpc GetPage(PageRequest) returns(PageResponse) {}
}

// message 訊息格式
message PageRequest{}
message PageResponse{
  string title = 1;
}